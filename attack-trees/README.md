# Zero Trust Attack Trees

## Building an Example Attack Tree

In order to render the Graphviz trees in this folder as PNG files, run the following from this directory:

```
./build-tree.sh confidentiality-tree
```